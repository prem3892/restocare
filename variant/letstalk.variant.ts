import { LetsTalkType } from '@/types/letstalk.type';

export const LetsTalkData: LetsTalkType = {
  heading: "Let's talk!",
  subHeading:
    'Restaurants don’t fail because the food is bad. They fail because the operations fall apart.',
  image: '/letstalk/letstalk.jpg',
};
